version: "3.9"

services:
  api:
    build: ./api
    ports:
      - "8080:8080"
    depends_on:
      - redis
      - ai-server

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  ai-server:
    build: ./ai-server
    ports:
      - "8000:8000"